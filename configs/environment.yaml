name: text-omni-flow
prefix: /120090727/yutong/conda_env/text-omni-flow
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch==2.11.0+cu128
      - transformers==5.14.1
      - peft==0.19.1
      - bitsandbytes==0.49.2
      - datasets==5.0.0
      - accelerate==1.14.0
      - pyyaml==6.0.3
      - safetensors==0.8.0
      - sentencepiece==0.2.2

# Install the CUDA build of PyTorch from:
#   pip install torch==2.11.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# Install the remaining pip dependencies from the default project index.
